/** Стыки стен — обёртка над legacy + core helpers. */
export {
  otherWallsAtPoint,
  teeHostAtPoint,
  wallAtPoint,
  stemTeeGapsOnSegment,
} from "../../wallJoins.js";

export { normalizeWalls, DEFAULT_MERGE_VERTEX_MM } from "./wallNormalize.js";
